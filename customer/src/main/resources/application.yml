server:
  port: 9090
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/roscadb
    username: root
    password: pwd_mysql
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: true
